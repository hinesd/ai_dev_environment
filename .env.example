# ─── Secrets ───────────────────────────────
# Password fallback for local CLI access (generated by `make init`).
OPENCLAW_GATEWAY_PASSWORD=

# DuckDNS API token — the domain is in settings.
# Get your token at https://duckdns.org
DUCKDNS_TOKEN=
